ConditionLatch final runtime handoff

Canonical repository: https://github.com/BeatyXO/conditionlatch
Immutable live fixture commit: a820417c7b4fd6c74f20d621fcc4801cbeec222b

The architecture, contracts, adversarial tests, documentation, repository hygiene, fixture pinning, and local static checks are already complete. Do not redesign the primitive, add a frontend, recreate fixtures, or redo work that is already present. Inspect the repository first and preserve the architecture unless a real GenLayer Direct Mode or stable Studionet failure proves that a compatibility fix is necessary.

Your remaining work is only the environment-dependent final verification and live proof:

1. Use Python 3.12+ and install the repository's pinned stable `genlayer-test==0.29.2`.
2. Run `python scripts/preflight.py` and `python -m compileall -q contracts tests scripts`.
3. Run the complete Direct Mode suite under `tests/direct/`. Fix only actual failures. Do not weaken assertions, validator independence, exact source-snapshot binding, fail-closed semantics, temporal determinism, append-only history, definition/generation pinning, or ConditionGate replay protection to make tests green.
4. Before every live write, verify the effective target is stable `studionet`, RPC `https://studio.genlayer.com/api`, chain ID `61999`. Do not switch networks as a workaround.
5. Run the real live lifecycle in `tests/integration/test_studionet_lifecycle.py` (adjust only current stable-tooling syntax if the installed stable tooling proves the documented invocation has changed). The immutable fixture is already pinned; do not replace its commit SHA with a mutable branch.
6. The live proof must produce real evidence for: ConditionLatch deployment; condition create/source/seal; non-empty 64-hex definition hash; three consensus-backed TRUE observations where a `CONSECUTIVE_TRUE=3` condition remains ACTIVE after rounds 1 and 2 and becomes LATCHED only after round 3; non-empty snapshot/round hashes; ConditionGate deployment against the finalized latch address; successful correctly pinned consumption; action replay rejection; wrong-definition rejection; and wrong-generation rejection. If current stable tooling exposes useful reset/generation-2 proof without compromising the canonical demonstration, record it as additional evidence.
7. Never invent an address, transaction hash, rejected transaction ID, test count, CI result, definition hash, snapshot hash, or round hash. If a rejected call has no transaction hash, record the exact real revert/error evidence instead.
8. Update only the evidence-bearing final docs as needed: `DEPLOYMENT.md`, `BUILD_STATUS.md`, `SUBMISSION.md`, and `proof/VERIFICATION_CHECKLIST.md`. Correct any command that current stable tooling proves inaccurate.
9. Final gate: `python scripts/preflight.py --final` must pass; Python compilation and all Direct Mode tests must pass; the live Studionet lifecycle must pass; no secret, `.env`, virtualenv, cache, artifact, frontend, or unrelated file may be tracked. Inspect git diff/status and the final GitHub HEAD, then push the final commit to `main`.

At the end report only real results: what runtime compatibility changes (if any) were necessary, exact Direct Mode results, live ConditionLatch/ConditionGate addresses, real transaction/revert evidence, definition/snapshot/round hashes, final 40-character Git commit SHA, and any genuine limitation that remains.
